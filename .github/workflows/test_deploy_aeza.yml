name: My-Project-CD

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Run Tests
        run: |
          echo "Hello from github-actions"
          whoami
          pwd
          ls
